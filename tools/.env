# Azure SQL connection for Bemel ETL
AZURE_SQL_SERVER=dbbemel.database.windows.net
AZURE_SQL_DATABASE=dbbemel
AZURE_SQL_USER=administrador
# Reemplaza con tu contraseña real
AZURE_SQL_PASSWORD=Pa$$w0rd123456
AZURE_SQL_AUTHENTICATION=SqlPassword
# Optional override for XML root (defaults to repo XMLS_COL if omitted)
XML_ROOT=/Users/luismaury/Codes/Bemel/XMLS_COL

# --- SFTP connection (used by tools/etl/sftp_fetch.py) ---
# From WinSCP profile: host 20.16.243.165, port 2229, user sftpdevtibacocw.cmconsulting
SFTP_HOST=20.16.243.165
SFTP_PORT=2229
SFTP_USER=sftpdevtibacocw.cmconsulting

# Choose ONE auth method below:
# 1) Password auth (uncomment and set the real password)
# SFTP_PASSWORD=your-password-here
# 2) Key auth with an OpenSSH PEM private key (recommended). If you have a .ppk,
#    convert it first to OpenSSH format, then point to that file.
#    We already include a key file in this repo; by default we point to it relatively.
#    Adjust path if you run on Windows outside this repo clone.
SFTP_KEY_FILE=tools/Sftp/forward-colombia
# If the key has a passphrase, set it here:
# SFTP_KEY_PASSPHRASE=your-key-passphrase
